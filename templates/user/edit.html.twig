{% extends 'base.html.twig' %}

{% block title %}Editar User
{% endblock %}

{% block body %}
	<h1 style="text-align:center">Editar User</h1>


<div class="container mt-5">
	<div class="row ">
		<div class="col-md-6">
			{{ include('user/_form.html.twig', {'button_label': 'Guardar'}) }}
		</div>

		<div class="col-md-6 align-self-end">
			<a href="{{ path('user_index') }}">Volver</a>
		</div>

		<div class="col-md-6 align-self-end">
			{{ include('user/_delete_form.html.twig') }}
		</div>

	</div>

</div>

{% endblock %}

