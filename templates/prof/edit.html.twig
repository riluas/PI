{% extends 'base.html.twig' %}

{% block title %}Editar Profesor
{% endblock %}

{% block body %}
	<h1 style="text-align:center">Editar Profesor</h1>

	<div class="container mt-5">
		<div class="row ">
			<div class="col-md-4 align-self-end">
				{{ include('prof/_form.html.twig', {'button_label': 'Guardar'}) }}
			</div>

			<div class="col-md-4 align-self-end">
				<a href="{{ path('prof_index') }}" class="btn">Volver</a>
			</div>

			<div class="col-md-4 align-self-end">
				{{ include('prof/_delete_form.html.twig') }}
			</div>

		</div>

	</div>
{% endblock %}
