{% extends 'base.html.twig' %}

{% block title %}Titulacion
{% endblock %}

{% block body %}
	<h1>Titulacion</h1>
	<div class="table-responsive">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ titulacion.id }}</td>
				</tr>
				<tr>
					<th>Nombre</th>
					<td>{{ titulacion.nombre }}</td>
				</tr>
			</tbody>
		</table>
	</div>


	<div class="container-fluid">
		<div class="row ">
			<div class="col-md-4">
				<a href="{{ path('titulacion_index') }}" class="btn">Volver</a>
			</div>

			<div class="col-md-4 align-self-end">
				<a href="{{ path('titulacion_edit', {'id': titulacion.id}) }}" class="btn">Editar</a>
			</div>

			<div class="col-md-4 align-self-end ">
				{{ include('titulacion/_delete_form.html.twig') }}
			</div</div>

		</div>
	</div>
{% endblock %}
