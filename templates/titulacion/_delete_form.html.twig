<form method="post" action="{{ path('titulacion_delete', {'id': titulacion.id}) }}" onsubmit="return confirm('Estas seguro que quieres eliminar esta titulación?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ titulacion.id) }}">
    <button class="btn">Eliminar</button>
</form>
